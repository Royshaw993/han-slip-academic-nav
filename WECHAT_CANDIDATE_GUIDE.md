# 微信公众号文章候选导入说明

## 1. 公众号入口是干什么的

这个入口用于把您主动提供的公开微信公众号文章链接加入现有候选池。工具会尽量读取文章标题、公众号名称、发布时间和作者，并根据项目已有规则匹配汉代简牍关键词及专题。

它不会搜索或批量扫描公众号历史文章，不使用登录、Cookie 或模拟登录，也不会绕过验证码及微信访问限制。

## 2. 如何复制公众号文章 URL

在微信中打开公开文章，点击右上角菜单，选择“复制链接”。通常得到的链接以 `https://mp.weixin.qq.com/s` 开头。请只导入您有权正常访问的公开文章，不要导入私人、登录后可见或受限制的内容。

## 3. 如何运行导入命令

在项目根目录打开命令提示符或 PowerShell，运行：

```powershell
python tools/import-wechat-candidate.py "https://mp.weixin.qq.com/s/文章链接"
```

如果电脑使用 Python 启动器，也可以把开头的 `python` 换成 `py -3`。

## 4. 如何一次导入多个 URL

把每个链接分别用英文双引号括起来，并用空格分隔：

```powershell
python tools/import-wechat-candidate.py "URL1" "URL2" "URL3"
```

为保持低频、人工提供链接的使用方式，每次最多接受 20 个 URL。每个链接只请求一次，不自动重试。

如只想检查命令和去重、不联网也不写文件，请加入 `--dry-run`：

```powershell
python tools/import-wechat-candidate.py --dry-run "URL1" "URL2"
```

## 5. 导入后去哪里看候选

新候选继续保存在：

```text
data/candidate-updates.json
```

最近 12 个月或日期未知的记录进入 `candidates`；超过最近 12 个月的记录进入 `olderCandidates`。工具也会在 `data/seen-items.json` 中登记已见链接，用于避免以后重复导入。

## 6. 日期无法识别怎么办

工具只接受页面中明确、可靠的发布时间。如果无法确认，不会猜测，也不会把文章当作最新内容，而是保存为空日期：

```json
"possibleDate": "",
"needsDateReview": true
```

请在人工审核时打开原文核实日期并补充。

## 7. 页面无法读取怎么办

微信可能要求验证、限制访问，或者页面已经失效。工具不会绕过这些限制，也不会因此放弃这个用户提供的链接，而会生成一个 `needsManualMetadata: true` 的候选框架。请之后人工填写标题、公众号、日期和作者。

页面无法读取不代表文章可以通过其他技术手段强行抓取。遇到登录、验证码、401、403、429 或访问验证时应停止自动读取。

## 8. 如何判断公众号是不是转载

人工审核时请查看标题附近是否标有“转载”“来源”“原载于”，文章末尾是否注明原刊物、高校、研究机构、出版社或数据库，并比较文中的原文链接、作者和发布日期。不要仅根据公众号名称猜测是否原创。

每条公众号候选都带有：

```json
"preferOriginalSource": true
```

这是审核提示，不代表工具已经判定文章属于转载。

## 9. 为什么优先寻找原始学术来源

正式期刊、高校、研究机构、出版社或数据库页面通常具有更稳定的题名、作者、日期和出处信息，也更适合长期引用。如果公众号只是转载，正式收录时应优先使用原始学术来源。

如果公众号本身就是研究中心首发、学术机构通知、独家讲座通知或原始书讯，经人工核实后可以保留公众号链接。

## 10. 不会自动发布网站

公众号导入只把记录写入候选池，不会修改 `academic-updates.js`，不会自动批准候选，不会执行 Git 提交或推送，也不会更新 GitHub Pages。所有公众号候选仍须人工审核后才能正式发布。
