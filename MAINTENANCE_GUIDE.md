# 古文字学术导航｜汉代简牍学术动态半自动维护 SOP

## 一、这套系统是怎么工作的

整个流程是：

**自动发现 → 生成候选 → 人工审核 → 你做决定 → Codex 写入 → 自动校验 → 网站更新**

重要原则：

> 自动发现 ≠ 自动发布。

任何新论文、新书、会议、数据库更新，都必须经过人工审核后，才能正式进入网站。

---

# 二、平时多久检查一次

建议：

**每周检查一次即可。**

例如固定：

> 每周日上午

如果某段时间正好有很多简帛会议、新资料公布，也可以临时多检查一次。

不建议每天高频抓取。

---

# 三、第一步：打开项目

打开 VS Code。

打开文件夹：

```text
古文字学术导航
```

目前重要文件大致包括：

```text
古文字学术导航
│
├── index.html
├── style.css
├── script.js
├── academic-updates.js
├── research-topics.js
├── REVIEW_RULES.md
│
├── tools
│   └── check-updates.py
│
└── data
    ├── sources.json
    ├── candidate-updates.json
    └── seen-items.json
```

---

# 四、第二步：运行半自动检查

在 VS Code 顶部：

**终端 → 新建终端**

首先确认 Python 正常：

```powershell
python --version
```

正常情况下会显示类似：

```text
Python 3.14.7
```

---

## 正式检查新动态

输入：

```powershell
python tools/check-updates.py
```

然后按回车。

系统会自动尝试检查目前配置的来源，例如：

* 武汉大学简帛网
* 清华大学出土文献研究与保护中心
* 西北师范大学简牍学术资源平台
* 中国考古网

程序只会低频访问公开页面。

它不会：

* 自动登录
* 绕过验证码
* 绕过 403 / 429
* 高频爬取
* 自动修改正式学术动态

---

# 五、第三步：看终端结果

运行结束后，会看到类似：

```text
汉代简牍学术动态检查完成

检查来源：4
成功访问：3
需要人工检查：1

发现页面：290
关键词匹配：7
已有动态：1
重复候选：6
新候选：2
```

重点看：

## 新候选

例如：

```text
新候选：3
```

说明发现了 3 条可能值得审核的新内容。

如果：

```text
新候选：0
```

那这一周基本不用再做什么。

---

## 需要人工检查

比如：

```text
中国考古网：SSL handshake timeout
```

这表示自动访问失败。

不是网站代码坏了。

以后有时间可以手动打开该网站查看。

---

# 六、第四步：打开候选文件

在 VS Code 左侧找到：

```text
data
└── candidate-updates.json
```

打开。

里面主要关注三个状态。

---

## A. candidates

例如：

```json
"candidates": [...]
```

这里是真正的：

> **近期候选**

也就是程序认为：

* 与汉代简牍相关
* 时间在最近约 12 个月
* 尚未收录

这些最值得审核。

---

## B. olderCandidates

例如：

```json
"olderCandidates": [...]
```

这里是：

> **超过最近12个月的旧资料**

它们不是“最新动态”。

但其中：

* 重要专著
* 整理报告
* 经典论文
* 重要数据库
* 研究史综述

仍然可能值得做：

> **历史补录**

---

## C. needsDateReview

如果某条显示：

```json
"needsDateReview": true
```

说明：

> 程序找到了相关页面，但是没办法可靠确定发布日期。

这种不要直接加入。

需要人工核验。

---

# 七、第五步：让 Codex 审核候选

这一步不要让 Codex直接修改网站。

推荐设置：

> **GPT-5.6 Luna + 中**

给 Codex：

```text
请按照项目根目录中的 REVIEW_RULES.md，
审核 data/candidate-updates.json 中本次发现的所有 candidates。

如果存在 olderCandidates，也请单独判断是否值得作为“历史补录”。

要求：

1. 读取现有 academic-updates.js 用于去重；
2. 核验标题、作者、日期、来源和原文 URL；
3. 按 REVIEW_RULES.md 的五项标准逐条评分；
4. 区分：
   - 正式加入
   - 可选加入
   - 历史补录
   - 暂不加入
   - 不建议加入
5. 不要修改任何文件；
6. 不要写入 academic-updates.js；
7. 先把审核结果给我看。

请逐条给出：

标题：
日期：
来源：
研究对象：

相关性：0/1
来源可靠性：0/1
学术价值：0/1
去重性：0/1
长期参考价值：0/1

总分：X/5

建议：
理由：

最后汇总：
建议正式加入 X 条
建议历史补录 X 条
可选 X 条
不建议加入 X 条
```

---

# 八、第六步：你做最终决定

Codex审核后，**最终决定权在你**。

推荐：

### 5分

> 强烈建议加入

### 4分

> 建议加入

### 3分

> 看情况

### 2分

> 通常不加入

### 0—1分

> 不加入

---

## 即使高分，也不能直接加入的情况

如果存在：

* 原始来源无法确认
* 作者不明确
* 日期冲突
* 标题错误
* URL 失效
* 已经重复
* 需要猜测关键信息

那么：

> 暂不加入。

---

# 九、第七步：告诉 Codex 你决定加入哪些

例如 Codex 审核了：

```text
1. 悬泉汉简某论文
2. 走马楼西汉简某论文
3. 某报名通知
4. 某旧书
```

你决定加入：

> 1、2、4

那么告诉 Codex：

```text
我确认正式加入第1、2条，
第4条作为历史补录加入。

第3条不加入。

请按照现有 academic-updates.js 的数据结构正式写入。

要求：

1. 不修改未被我批准的候选；
2. 为每条新增动态创建唯一 id；
3. 再次核验 sourceUrl；
4. 更新 academicUpdatesLastUpdated 为今天日期；
5. 运行 validateAcademicUpdates()；
6. 检查是否存在重复 id；
7. 检查 title + source 是否重复；
8. 检查研究对象筛选是否识别；
9. 检查对应专题数量是否自动更新；
10. 不修改其他资源、布局或 CSS。

完成后告诉我新增了哪些内容和最终校验结果。
```

---

# 十、第八步：正式写入时用什么模型

如果已经完成审核，只剩：

> 把确认的几条写进网站

推荐：

**GPT-5.4 Mini + 轻度**

因为这一步逻辑简单，不需要浪费 Codex 高额度。

如果需要：

* 增加新的研究对象
* 修改专题匹配
* 调整筛选

再使用：

**GPT-5.6 Luna + 中**

---

# 十一、第九步：检查 Codex 最终报告

正常情况下你应该看到类似：

```text
新增3条真实动态

总动态：18条

Academic updates loaded:
18 items, 0 warnings
```

重点确认：

* 总数量正确
* `0 warnings`
* 无重复 ID
* URL 都是 HTTPS
* `academicUpdatesLastUpdated` 已更新

---

# 十二、第十步：自己打开网站检查

打开：

```text
index.html
```

重点检查：

### 最新学术动态

新内容有没有出现。

### 专题

例如新增悬泉汉简论文后：

> 悬泉汉简专题

数量是否自动增加。

### 筛选

尝试：

> 新论文 + 悬泉汉简

看看结果是否正常。

### 日期

确认：

> 数据更新：今天的日期

以及：

> 最近事件：对应数据中的最大事件日期

注意：

**“最近事件”不是网站更新时间。**

---

# 十三、如果发现候选都是旧内容

如果：

```json
"candidates": []
```

但：

```json
"olderCandidates": [...]
```

说明：

> 这一轮没有最新内容，只发现了一些旧资料。

不用硬加。

只有真正有长期价值的：

* 专著
* 整理成果
* 数据库
* 经典论文
* 重要综述

才考虑：

> 历史补录。

---

# 十四、如果 candidates 为 0

非常简单：

**什么都不用做。**

这代表：

> 本次自动检查没有发现值得进入人工审核阶段的新动态。

下一周再运行即可。

---

# 十五、如果某个网站自动检查失败

例如：

```text
中国考古网
SSL handshake timeout
```

不要让程序：

* 强行重试几十次
* 绕过安全验证
* 破解验证码

可以：

> 手动打开官网检查。

如果网站长期无法自动访问，可以继续在：

```text
sources.json
```

保留：

```text
manualReview: true
```

---

# 十六、千万不要做的事情

不要直接告诉 Codex：

> “把所有找到的内容全部加入网站。”

不要。

正确流程永远是：

```text
发现
↓
候选
↓
审核
↓
你决定
↓
写入
```

---

# 十七、最简版操作流程

如果以后忘了所有细节，只记住下面 5 步：

## ① 检查

```powershell
python tools/check-updates.py
```

## ② 看候选

打开：

```text
data/candidate-updates.json
```

## ③ 让 Codex 审核

告诉它：

```text
请按照 REVIEW_RULES.md 审核 candidate-updates.json，
不要修改任何文件。
```

## ④ 你决定

例如：

```text
加入1、3、5。
2、4不加入。
```

## ⑤ 让 Codex 写入

告诉它：

```text
请将我确认的候选写入 academic-updates.js，
更新 academicUpdatesLastUpdated，
运行 validateAcademicUpdates()，
不要修改其他内容。
```

完成。

---

# 十八、建议的每周维护习惯

建议固定：

## 每周一次

大约 10—20 分钟。

流程：

```text
运行检查
   ↓
有没有新候选？
   ↓
没有 → 结束

有
   ↓
Codex审核
   ↓
自己确认
   ↓
正式加入
   ↓
打开网页确认
```

不用每天维护。

---

# 十九、Codex 模型怎么选

### 自动发现后的候选审核

**GPT-5.6 Luna + 中**

### 写入1—5条已经确认的数据

**GPT-5.4 Mini + 轻度**

### 新增专题 / 修改筛选

**GPT-5.6 Luna + 中**

### 抓取脚本出现复杂故障

**GPT-5.6 Terra + 中**

### 非常复杂的架构/难查 Bug

**GPT-5.6 Sol + 高**

日常不要一直使用 Sol + 高。

---

# 二十、你的数据安全底线

无论以后怎么升级，始终坚持：

> `candidate-updates.json`
>
> 是机器发现的候选。

↓

> `REVIEW_RULES.md`
>
> 是审核标准。

↓

> **你**
>
> 是最终决定者。

↓

> `academic-updates.js`
>
> 才是正式发布的数据。

这四层不要合并。

这能最大限度避免：

* AI误判
* 假论文
* 旧新闻当新动态
* 重复收录
* 错误链接
* 低价值信息污染网站

---

# 一句话记忆版

> **运行 Python 找候选 → Codex 按规则审核 → 我决定加哪些 → Codex 写入正式数据 → 打开网页检查。**

这就是整个半自动维护流程。
